function Ejercicio33() {
    let resp;
  
    do {
      resp = prompt('¿Desea continuar? (S/N): ');
      resp = resp.toUpperCase();
    } while (resp === 'S');
  
    // Puedes agregar más acciones después del bucle si es necesario.
  }
  
  Ejercicio33();
  